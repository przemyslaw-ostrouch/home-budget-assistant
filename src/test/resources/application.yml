server:
  port: 8080

spring:
  datasource:
#    url: "jdbc:h2:mem:./budget-assistant"
    url: "jdbc:h2:mem:"
    username: "test"
    initialization-mode: always
  jpa:
    properties:
      hibernate:
        hbm2ddl:
          auto: create-drop
    show-sql: true
